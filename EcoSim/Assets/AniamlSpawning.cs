using UnityEngine;
using System.Collections;

public class AniamlSpawning : MonoBehaviour {

    public GameObject baseAnimal;


	// Use this for initialization
    void Start()
    {
        //int randNoAnimals = Random.Range(0, 100);
        //for (int i = 0; i < randNoAnimals; i++)
        //{
        //    Rigidbody animalClone = (Rigidbody)Instantiate(baseAnimal, transform.position, transform.rotation);

        //}
    }
	
	
	// Update is called once per frame
	void Update () {
	
	}
}
